# Cat3 Closing

Close the guided build by naming the IB concepts `{ib_key_concepts}` and connecting them to the child's sequence of typed build reports.

Keep the goodbye warm, brief, and text-only. Offer a next-time variation using the same build mechanic.
