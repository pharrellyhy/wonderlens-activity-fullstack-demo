dashboard_fragment:
  session:
    axis: form
    angle: pattern
    mechanic: collect
    entity_role: catalyst
    focal_attribute: "beginning_b_sound"
    entry_rung: "{runtime_entry_rung}"
    exit_rung: "{runtime_exit_rung}"
    outcome: "{runtime_outcome}"

  contributes_to:
    curiosity_radial:
      axis: form
      angle: pattern
    exploration_matrix:
      cell: "collect by beginning sound"
    key_concepts_exposure:
      Form:
        angle: pattern
      Connection:
        angle: pattern
    atl_skills_trail:
      - language_expression
      - observation
      - classification
